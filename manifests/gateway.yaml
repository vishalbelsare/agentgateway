apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-gw
spec:
  selector:
    matchLabels:
      app: mcp-gw
  template:
    metadata:
      labels:
        app: mcp-gw
    spec:
      containers:
        - name: mcp-gw
          image: localhost:5000/mcp-gw:latest
          command:
            - mcp-gw
            - --config=/config/config.yaml
          volumeMounts:
            - name: config-volume
              mountPath: /config
      volumes:
        - name: config-volume
          configMap:
            name: mcp-gw-config
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-gw
spec:
  selector:
    app: mcp-gw
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-gw-config
data:
  config.yaml: |
    listener:
      sse:
        host: 0.0.0.0
        port: 8001
    rules: []
    targets:
      local:
        stdio:
          args:
          - '@modelcontextprotocol/server-everything'
          cmd: npx

#      remote:
#        sse:
#          host: mcp-everything
#          port: 80
